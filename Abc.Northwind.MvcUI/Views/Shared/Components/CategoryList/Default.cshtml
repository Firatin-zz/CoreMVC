@model Abc.Northwind.MvcUI.Models.CategoryListVM

<div class="list-group">
    @foreach (var item in Model.Categories)
    {
        <a href="#" class="list-group-item">@item.CategoryName</a>
    }
</div>